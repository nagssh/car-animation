// Add this JavaScript code to your car.js file or within a script tag in your HTML file

// Wait for the DOM to load
window.addEventListener('DOMContentLoaded', (event) => {
    // Get the car element
    const carElement = document.querySelector('.car');

    // Start the animation by adding the 'running' class to the car element
    carElement.classList.add('running');
});
